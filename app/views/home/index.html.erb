<div class="min-h-screen bg-gradient-to-br from-surface via-surface-elevated to-surface">
  <!-- Hero Section -->
  <div class="container mx-auto px-4 pt-20 pb-32">
    <div class="text-center max-w-4xl mx-auto">
      <!-- Logo -->
      <div class="flex justify-center mb-8">
        <svg width="80" height="80" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect width="200" height="200" rx="40" fill="url(#gradient)"/>
          <path d="M60 140L80 60L100 120L120 80L140 140" stroke="white" stroke-width="12" stroke-linecap="round" stroke-linejoin="round"/>
          <defs>
            <linearGradient id="gradient" x1="0" y1="0" x2="200" y2="200">
              <stop offset="0%" stop-color="#07c160"/>
              <stop offset="100%" stop-color="#00a854"/>
            </linearGradient>
          </defs>
        </svg>
      </div>

      <!-- Heading -->
      <h1 class="text-5xl md:text-6xl font-bold text-foreground mb-6 tracking-tight">
        WeMD 编辑器
      </h1>
      
      <p class="text-xl text-muted-foreground mb-12 max-w-2xl mx-auto">
        专为微信公众号打造的 Markdown 排版编辑器<br>
        所见即所得，一键复制到公众号后台，让排版更简单
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-wrap gap-4 justify-center">
        <% if Current.user %>
          <%= link_to documents_path, class: "btn-primary btn-lg" do %>
            <%= lucide_icon "file-text", class: "w-5 h-5 mr-2" %>
            我的文档
          <% end %>
          <%= link_to new_document_path, class: "btn-outline btn-lg" do %>
            <%= lucide_icon "plus", class: "w-5 h-5 mr-2" %>
            新建文档
          <% end %>
        <% else %>
          <%= link_to sign_up_path, class: "btn-primary btn-lg" do %>
            <%= lucide_icon "user-plus", class: "w-5 h-5 mr-2" %>
            开始使用
          <% end %>
          <%= link_to sign_in_path, class: "btn-outline btn-lg" do %>
            <%= lucide_icon "log-in", class: "w-5 h-5 mr-2" %>
            登录
          <% end %>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Features Section -->
  <div class="container mx-auto px-4 pb-20">
    <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
      <!-- Feature 1 -->
      <div class="card text-center">
        <div class="card-body">
          <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary/10 mb-6">
            <%= lucide_icon "zap", class: "w-8 h-8 text-primary" %>
          </div>
          <h3 class="text-xl font-semibold text-foreground mb-3">实时预览</h3>
          <p class="text-muted-foreground">
            Markdown 编辑的同时，实时预览微信公众号排版效果，所见即所得
          </p>
        </div>
      </div>

      <!-- Feature 2 -->
      <div class="card text-center">
        <div class="card-body">
          <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary/10 mb-6">
            <%= lucide_icon "palette", class: "w-8 h-8 text-primary" %>
          </div>
          <h3 class="text-xl font-semibold text-foreground mb-3">主题定制</h3>
          <p class="text-muted-foreground">
            多套精美主题可选，支持自定义 CSS 样式，打造独特的文章风格
          </p>
        </div>
      </div>

      <!-- Feature 3 -->
      <div class="card text-center">
        <div class="card-body">
          <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary/10 mb-6">
            <%= lucide_icon "copy", class: "w-8 h-8 text-primary" %>
          </div>
          <h3 class="text-xl font-semibold text-foreground mb-3">一键复制</h3>
          <p class="text-muted-foreground">
            自动转换为带样式的富文本，直接粘贴到微信公众号后台，无需二次调整
          </p>
        </div>
      </div>
    </div>
  </div>

  <!-- How It Works Section -->
  <div class="container mx-auto px-4 pb-32">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-3xl md:text-4xl font-bold text-center text-foreground mb-16">
        简单三步，完成排版
      </h2>
      
      <div class="space-y-12">
        <!-- Step 1 -->
        <div class="flex items-start gap-6">
          <div class="flex-shrink-0 w-12 h-12 rounded-full bg-primary text-white flex items-center justify-center text-xl font-bold">
            1
          </div>
          <div>
            <h3 class="text-xl font-semibold text-foreground mb-2">编写 Markdown</h3>
            <p class="text-muted-foreground">使用熟悉的 Markdown 语法编写文章，支持标题、列表、引用、代码块等所有常用格式</p>
          </div>
        </div>

        <!-- Step 2 -->
        <div class="flex items-start gap-6">
          <div class="flex-shrink-0 w-12 h-12 rounded-full bg-primary text-white flex items-center justify-center text-xl font-bold">
            2
          </div>
          <div>
            <h3 class="text-xl font-semibold text-foreground mb-2">选择主题</h3>
            <p class="text-muted-foreground">从多套精美主题中选择，或自定义 CSS 样式，实时预览排版效果</p>
          </div>
        </div>

        <!-- Step 3 -->
        <div class="flex items-start gap-6">
          <div class="flex-shrink-0 w-12 h-12 rounded-full bg-primary text-white flex items-center justify-center text-xl font-bold">
            3
          </div>
          <div>
            <h3 class="text-xl font-semibold text-foreground mb-2">复制粘贴</h3>
            <p class="text-muted-foreground">点击"复制到公众号"按钮，直接粘贴到微信公众号编辑器，保留所有样式</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- CTA Section -->
  <div class="container mx-auto px-4 pb-20">
    <div class="card bg-primary/5 border-2 border-primary/20 max-w-4xl mx-auto">
      <div class="card-body text-center py-16">
        <h2 class="text-3xl font-bold text-foreground mb-4">
          准备好开始了吗？
        </h2>
        <p class="text-muted-foreground mb-8 text-lg">
          立即注册，体验专业的微信公众号排版工具
        </p>
        <% if Current.user %>
          <%= link_to new_document_path, class: "btn-primary btn-lg" do %>
            <%= lucide_icon "edit", class: "w-5 h-5 mr-2" %>
            开始创作
          <% end %>
        <% else %>
          <%= link_to sign_up_path, class: "btn-primary btn-lg" do %>
            <%= lucide_icon "user-plus", class: "w-5 h-5 mr-2" %>
            免费注册
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
