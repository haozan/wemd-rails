<div class="container-sm flex items-center justify-center flex-1 py-12">
  <div class="card w-full">
    <div class="card-body">
      <h1 class="text-3xl font-bold text-center text-primary mb-8">重置密码</h1>

      <%= form_with model: @user, url: password_path(token: params[:token]), method: :patch, local: true, class: "space-y-6" do |form| %>
        <div>
          <%= form.label :password, required: true do %>
            新密码
          <% end %>
          <%= form.password_field :password, required: true, autofocus: true, autocomplete: "new-password", placeholder: "请输入新密码" %>
        </div>

        <div>
          <%= form.label :password_confirmation, required: true do %>
            确认密码
          <% end %>
          <%= form.password_field :password_confirmation, required: true, autocomplete: "new-password", placeholder: "请再次输入新密码" %>
        </div>

        <div class="pt-2">
          <%= form.submit "保存更改", class: "btn-primary w-full btn-lg" %>
        </div>
      <% end %>

      <div class="text-center mt-8">
        <%= link_to "返回登录", sign_in_path, class: "text-primary hover:text-primary-dark underline font-medium transition-colors" %>
      </div>
    </div>
  </div>
</div>