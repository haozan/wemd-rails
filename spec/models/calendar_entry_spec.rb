require 'rails_helper'

RSpec.describe CalendarEntry, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:calendar_entry)
  #   expect(record).to be_persisted
  # end
end
